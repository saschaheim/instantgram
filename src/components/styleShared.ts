import { uiAnimations, uiClasses, uiIds } from "./uiTokens";

export const cssLinearGradient = `background:#f6e2d8!important;background:linear-gradient(45deg,#f6e2d8 0%,#68c2e8 100%)!important;`;

export const sharedUiClasses = uiClasses;
export const sharedUiIds = uiIds;
export const sharedUiAnimations = uiAnimations;
